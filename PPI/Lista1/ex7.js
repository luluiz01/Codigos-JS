/**Substitua todas as linhas (menos a primeira e a última) por uma única instrução contendo um operador ternário, mantendo a mesma lógica do algoritmo. */
let x = 4;
let y = (x == 3)? 'tres': 'não tres';

console.log(y);